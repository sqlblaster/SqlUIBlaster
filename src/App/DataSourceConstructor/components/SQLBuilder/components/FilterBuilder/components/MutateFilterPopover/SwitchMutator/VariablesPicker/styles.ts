import { StyleRulesCallback } from '@material-ui/core';

export type VariablesPickerClassKeys = 'button';

export const variablesPickerStyles: StyleRulesCallback<
  VariablesPickerClassKeys
> = (theme) => ({
  button: {
    padding: 5
  }
});
